export type LiteDislikeType = {
    _id: string,
    commentId?: string
    videoId?: string,
    userId: string
}

export type LikeDislikeDataType = {
    itemName: string,
    itemId: string,
    userId: string
}
